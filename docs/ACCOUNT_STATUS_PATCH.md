# Account footer and status badge patch

- Fixes the campaign status badge wrapping issue by adding a dedicated status cell width and non-wrapping badge styles.
- Moves the logged-in account display and logout action from the top banner to the bottom-left sidebar account area.
- Removes the top login success banner from the authenticated admin view.
